# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
artifacts
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Exported résumés. Printing is the export, so PDFs get saved next to the project
# while testing it — and one carrying real personal details was committed by a
# blanket `git add -A`. Only the root is ignored; nothing in the tree is a PDF.
/*.pdf
