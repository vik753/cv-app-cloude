/* The app layer has one entry point and one export: the root component main.tsx mounts.
   The providers and the preference wiring behind it are the layer's own business. */
export { App } from "@/app/App";
