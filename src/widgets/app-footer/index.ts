/* The footer takes nothing but the translation object; the author, the address and the
   year are its own constants, and the track credit it renders reads MUSIC from
   shared/config — the same record the scene's player is configured from. */
export { AppFooter } from "@/widgets/app-footer/ui/AppFooter";
