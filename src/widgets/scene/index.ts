/* The outside of this widget needs two things and nothing else: the scene itself and
   its soundtrack player. The thirteen figure components, the cycle clock and the
   landscape maths stay internal — that is what lets them be rearranged without a
   ripple. */
export { DayNightScene } from "@/widgets/scene/ui/DayNightScene";
export { SceneMusic } from "@/widgets/scene/ui/SceneMusic";
