/* The switch only, without the EN/УКР button labels beside it: those are the control's
   own presentation, not copy, which is why they live here and not in the i18n
   dictionaries. Language and the translations come from shared/i18n. */
export { LanguageSwitcher } from "@/features/language-switch/ui/LanguageSwitcher";
