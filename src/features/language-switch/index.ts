/* The segmented switch, and the same choice as menu rows for the header's one menu on a
   narrow screen. The EN/УКР button labels are not copy — "EN" is not translated — so
   they live in this feature rather than in the i18n dictionaries. Language and the
   translations come from shared/i18n. */
export { LanguageSwitcher } from "@/features/language-switch/ui/LanguageSwitcher";
export { LanguageOptions } from "@/features/language-switch/ui/LanguageOptions";
